import React from 'react';
import { StyledHeroSectionContainer } from './HeroSection.styles';

const HeroSection = (): JSX.Element => (
  <StyledHeroSectionContainer>
    <p>Hero Section</p>
  </StyledHeroSectionContainer>
);

export default HeroSection;
